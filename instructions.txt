conda env create -f ./fedsage.yml -n fedsage
conda activate fedsage
--------------------------------
conda update conda

$ conda create -n M1Max  python=3.8

$ conda activate M1Max

$ conda install -y clang_osx-arm64 clangxx_osx-arm64 gfortran_osx-arm64

$ MACOSX_DEPLOYMENT_TARGET=13.4 CC=clang CXX=clang++ python -m pip --no-cache-dir   install torch torchvision torchaudio

$ python -c "import torch; print(torch.__version__)"  #---> (Confirm the version is 1.11.0)

$ MACOSX_DEPLOYMENT_TARGET=13.4 CC=clang CXX=clang++ python -m pip --no-cache-dir  install  torch-scatter -f https://data.pyg.org/whl/torch-1.11.0+${cpu}.html

$ MACOSX_DEPLOYMENT_TARGET=13.4 CC=clang CXX=clang++ python -m pip --no-cache-dir  install  torch-sparse -f https://data.pyg.org/whl/torch-1.11.0+${cpu}.html

$ MACOSX_DEPLOYMENT_TARGET=13.4 CC=clang CXX=clang++ python -m pip --no-cache-dir  install  torch-geometric
